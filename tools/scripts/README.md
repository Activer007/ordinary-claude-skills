# 📊 Skills 批量分析脚本使用说明

## 简介

`analyze_all_skills.py` 是一个全量技能质量评分分析脚本，用于批量分析 skills_all 目录下的所有技能（415个），并生成详细的分析报告。

## 功能特性

- ✅ 自动扫描并分析所有技能
- ✅ 基于多维度评分系统（内容质量、技术实现、维护性、用户体验）
- ✅ 生成多种格式的分析报告
- ✅ 错误处理和进度显示
- ✅ 高性能：平均 0.01 秒/技能

## 使用方法

### 1. 激活虚拟环境

```bash
cd tools
source .venv/bin/activate
```

### 2. 运行分析脚本

```bash
python scripts/analyze_all_skills.py
```

### 3. 查看生成的报告

所有报告生成在 `reports/` 目录下：

```
reports/
├── skills_analysis_full_report.json  # 完整的JSON数据
├── skills_analysis_summary.md        # 可读的分析报告
├── top_100_skills.md                 # Top 100 高分技能推荐
└── improvement_candidates.md         # 待改进技能清单
```

## 生成的报告说明

### 1. 完整数据报告 (`skills_analysis_full_report.json`)

包含所有技能的详细评分数据，适合程序化处理和数据分析。

**结构：**
```json
{
  "metadata": {
    "generated_at": "2026-01-13T15:46:10",
    "total_skills": 415,
    "analyzed_skills": 415,
    "failed_skills": 0
  },
  "statistics": {
    "average_score": 61.6,
    "median_score": 65,
    "grade_distribution": {...},
    ...
  },
  "results": [...]
}
```

### 2. 可读分析报告 (`skills_analysis_summary.md`)

Markdown 格式的分析报告，包含：
- 总体统计
- 评分等级分布
- 评分区间可视化
- Top 10 高分技能
- Bottom 10 低分技能

### 3. Top 100 技能推荐 (`top_100_skills.md`)

按评分排序的高质量技能推荐列表，包含：
- 技能名称、评分、等级、路径
- 等级分布统计
- 使用建议

### 4. 待改进技能清单 (`improvement_candidates.md`)

评分低于 60 分的技能列表，包含：
- 技能名称、当前评分、主要问题
- 常见问题统计
- 详细改进建议
- 预期提升效果

## 分析维度与评分标准

### 评分体系（100分制）

1. **内容质量 (50分)**
   - 指令清晰度 (13分)
   - 技术深度 (19分)
   - 文档完整度 (13分)
   - 可操作性 (5分)

2. **技术实现 (30分)**
   - 代码质量 (15分)
   - 模式设计 (10分)
   - 错误处理 (5分)

3. **维护性 (10分)**
   - 更新频率 (3分)
   - 社区活跃度 (5分)
   - 兼容性 (2分)

4. **用户体验 (10分)**
   - 易用性 (5分)
   - 可读性 (5分)

### 评级标准

- **S 级 (90-100分)**: 顶级质量，强烈推荐使用
- **A 级 (80-89分)**: 优秀质量，推荐使用
- **B 级 (70-79分)**: 良好质量，可以使用
- **C 级 (60-69分)**: 及格质量，建议优化
- **D 级 (0-59分)**: 需要改进

## 分析结果概览

**最新分析结果 (2026-01-13):**

- **总技能数**: 415
- **平均分**: 61.6/100
- **中位数分**: 65/100
- **最高分**: 88/100 (python-packaging)
- **最低分**: 3/100 (template-skill)

**评级分布：**
- S 级: 0 (0.0%)
- A 级: 43 (10.4%)
- B 级: 118 (28.4%)
- C 级: 103 (24.8%)
- D 级: 151 (36.4%)

## 技术细节

### 性能指标

- 分析速度: ~0.01 秒/技能
- 415 个技能总耗时: ~3.7 秒
- 内存使用: 低于 100MB

### 特性

- **懒加载**: SkillDocument 使用懒加载机制，提高性能
- **缓存**: 解析结果缓存，避免重复计算
- **错误处理**: 遇到问题的技能会单独记录，不影响整体分析
- **JSON 序列化**: 自定义编码器处理日期对象

## 故障排查

### 问题：找不到配置文件

**错误信息：**
```
FileNotFoundError: [Errno 2] No such file or directory: 'tools/config/scoring_weights.json'
```

**解决方法：**
确保在正确的目录下运行脚本，配置文件路径应为：
```
tools/config/scoring_weights.json
```

### 问题：虚拟环境未激活

**错误信息：**
```
ModuleNotFoundError: No module named 'analyzer'
```

**解决方法：**
激活虚拟环境：
```bash
source tools/.venv/bin/activate
```

### 问题：权限错误

**错误信息：**
```
PermissionError: [Errno 13] Permission denied: 'reports/...'
```

**解决方法：**
确保 reports/ 目录有写权限：
```bash
chmod -R u+w reports/
```

## 扩展使用

### 自定义评分阈值

如需修改待改进技能的评分阈值，可以编辑主函数：

```python
# 原始：threshold=60 (默认)
analyzer.save_improvement_candidates(threshold=60)

# 修改为70分
analyzer.save_improvement_candidates(threshold=70)
```

### 自定义 Top N 数量

修改 Top 技能列表的数量：

```python
# 原始：top_n=100
analyzer.save_top_skills(top_n=100)

# 修改为 Top 50
analyzer.save_top_skills(top_n=50)
```

### 集成到 CI/CD

在 GitHub Actions 中自动运行分析：

```yaml
name: Skills Quality Analysis

on:
  schedule:
    - cron: '0 0 * * 0'  # 每周日运行
  workflow_dispatch:

jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: |
          cd tools
          python -m venv .venv
          source .venv/bin/activate
          pip install -r requirements.txt
      - name: Run analysis
        run: |
          cd tools
          source .venv/bin/activate
          python scripts/analyze_all_skills.py
      - name: Upload reports
        uses: actions/upload-artifact@v2
        with:
          name: analysis-reports
          path: reports/
```

## 相关文档

- [评分系统设计](../../check-result.md)
- [任务说明](../../task.md)
- [项目说明](../../CLAUDE.md)

## 许可证

MIT License
