# Skills 自动化评分工具依赖项

# 网络请求
requests>=2.32.0     # HTTP 请求（GitHub 下载）

# 文本处理
pyyaml>=6.0.0        # 解析 YAML 前置元数据
regex>=2023.0.0      # 正则表达式增强

# 终端输出
tabulate>=0.9.0      # 表格格式化
colorama>=0.4.0      # 彩色终端输出

# 测试框架（可选）
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-benchmark>=4.0.0
